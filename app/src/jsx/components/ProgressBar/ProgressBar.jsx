import styles from "./styles.pcss";
import React, { Component } from "react";

class ProgressBar extends Component {
	render() {
		return (
			<div className={`${styles.this} js-progressBar`}>
				<div />
			</div>
		);
	}
}

export default ProgressBar;
